# Required: set these in your shell before running live/graber services
# NEVER commit actual values
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# NN training parameters (set before running nn-train)
NN_CLS=
NN_TGT=
NN_TYPE=
